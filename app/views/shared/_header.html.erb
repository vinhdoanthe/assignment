<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to image_tag('logofunix.png', :size => "80x50"), root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
        <li>
          <p>
            <%= current_user.email %>
          </p>
        </li>
        <li class="ml-3">
          <%= link_to t('devise.sessions.logout_btn'), destroy_user_session_path, method: :delete, class: 'btn btn-secondary' %>
        </li>
      <% else %>
        <%= link_to t('devise.sessions.login_btn'), new_user_session_path, class: 'btn btn-primary' %>
      <% end %>
    </ul>
  </div>
</nav>